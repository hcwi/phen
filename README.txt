PHEN PROJECT FOR WIN x64

To run the project R (http://www.r-project.org/) environment is required. 
Install R and set up a system variable R_HOME for the R home folder, so that Rscript.exe can be found as 
R_HOME/bin/x64/Rscript.exe